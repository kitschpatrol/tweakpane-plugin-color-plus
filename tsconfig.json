{
	"extends": "@kitschpatrol/typescript-config",
	"compilerOptions": {
		// TODO maybe remove on @kitschpatrol/shared-config >8.1.0
		"exactOptionalPropertyTypes": false
	},
	// Includes project dot-files files for typescript-eslint integration
	"include": ["**/*", "**/**.*"],
	"exclude": ["**/dist/"]
}
